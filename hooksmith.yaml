# Git hook configuration for hooksmith: https://github.com/TomPlanche/hooksmith
pre-commit:
  commands:
    - pnpm run format:check
    - pnpm run lint
    - pnpm run check
pre-push:
  commands:
    - pnpm run build:all
