# Global

- [ ] Route-scoped TODOs: [`/play`](src/routes/play/README.md), [`/daily`](src/routes/daily/README.md).
- [ ] Self-host: run my own lexicon resolution and PDS on my VPS instead of relying on a third party, and do the rest of
      the ATProto server-side setup that requires (DID, the `_lexicon` DNS TXT record, hosting the `com.tomplanche.atkuzu.*`
      records).

# Header

- [x] When connected, link the handle to the user's pdsls link, so they can see their own results and stats.
